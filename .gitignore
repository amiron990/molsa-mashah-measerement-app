# Local working files — not part of the deployed app
*.pdf
_reference/
_t.html

# Raw source data (compiled into assets/data.js by tools/gen_data.py).
# נתונים.csv sits here; the two dimension files live in the parent folder.
*.csv
